function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0,0);

  fill(150);

  triangle(0,height, width, height, width/2, height - 285)

}

// function mousePressed() {
//   save();
// }